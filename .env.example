# github-to-wechat 配置模板
#
# 用法：复制本文件为 .env，按需填写
#   cp .env.example .env        (Git Bash / macOS / Linux)
#   copy .env.example .env      (Windows CMD)
#
# .env 含密钥和本机路径，已被 .gitignore 忽略，不会提交。
#
# 全部变量都是可选的 —— 不配就用默认值，skill 照样能跑。

# ===== 生图后端（可选）=====
# 不配置则使用宿主 AI 工具自带的生图能力。

# OpenAI 兼容的 API 地址（下方为 Agnes 示例）
IMAGE_API_BASE=https://api.agnes-ai.cn

# 从供应商控制台获取 API Key
IMAGE_API_KEY=sk-xxxx

# 图片模型 ID（下方为 Agnes 的三个模型）
#   agnes-image-2.5-flash   版本最高，优先使用
#   agnes-image-2.1-flash
#   agnes-image-2.0-flash
IMAGE_MODEL=agnes-image-2.5-flash

# ===== 运行时路径（可选）=====
# 默认用当前 Python 解释器 / PATH 上的 node，通常不需要配置。
# 仅在运行时不在 PATH 中（隔离环境、版本管理器等）时填写绝对路径。

# PYTHON_BIN=python3
# NODE_BIN=node

# sharp 的 node_modules 位置。默认从 <SKILL_DIR>/node_modules 解析，
# 没有时会自动执行一次 npm install，所以通常不需要配置。
# 只有想把 sharp 放在别处时才取消注释。
# NODE_MODULES=/path/to/node_modules

# ===== 输出目录 =====
# 不可配置。文章固定输出到「当前工作区」根目录下的 articles/ —— 运行命令时
# 你在哪个工作区，就写到哪个工作区。想知道当前落点，跑一句：
#   python scripts/paths.py show

# ===== 去 AI 味（可选）=====
# 去 AI 味 skill 的 SKILL.md 绝对路径（例如 humanizer）。
# 配置后才会在第 5 步执行改写，未配置则跳过。
# HUMANIZER_SKILL=/path/to/humanizer/SKILL.md
