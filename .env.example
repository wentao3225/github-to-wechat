# github-to-wechat 配置模板
#
# 用法：复制本文件为 .env，按需填写
#   cp .env.example .env        (Git Bash / macOS / Linux)
#   copy .env.example .env      (Windows CMD)
#
# .env 含密钥和本机路径，已被 .gitignore 忽略，不会提交。
#
# 全部变量都是可选的 —— 不配就用默认值，skill 照样能跑。

# ===== 生图后端 =====

# Agnes API 地址，一般不用改
IMAGE_API_BASE=https://api.agnes-ai.cn

# 从 https://platform.agnes-ai.cn 控制台 → API Key 管理 获取
# 不配则用工具内置生图模型（可能消耗额度）
IMAGE_API_KEY=sk-xxxx

# 图片模型 ID。Agnes 当前图片模型免费，推荐用最高的 2.5
#   agnes-image-2.5-flash   推荐，版本最高
#   agnes-image-2.1-flash
#   agnes-image-2.0-flash
IMAGE_MODEL=agnes-image-2.5-flash

# ===== 运行时路径 =====
# 默认用 python3 / node，开箱即用。
# 只有运行时装在非标准位置（隔离环境、版本管理器等）才需要写绝对路径。
# 例：PYTHON_BIN=C:/Users/you/venv/Scripts/python.exe

# PYTHON_BIN=python3
# NODE_BIN=node

# ===== 文章产物目录 =====
# 不配则输出到「当前工作区/articles/」。
# 想固定存到某个目录就取消注释（Windows 路径用正斜杠）：
# WECHAT_ARTICLES_DIR=D:/my-blog/articles

# ===== 去 AI 味（可选） =====
# humanizer 的 SKILL.md 绝对路径。配了才会在第 5 步做去 AI 味，
# 没配就跳过（初稿直接当定稿）。
# 例：HUMANIZER_SKILL=C:/Users/you/.agents/skills/humanizer/SKILL.md
